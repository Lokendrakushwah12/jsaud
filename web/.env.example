# Leave unset for local dev — vite proxies /api to http://localhost:3001.
# Set this when the web app and the API are deployed to different origins.
VITE_API_URL=https://your-api-deployment.vercel.app
